Windows packaging notes

- Archives are produced as zip files containing the `.exe` and supporting files.
- Extract the full archive before launching the GUI so the bundled `img/` directory stays adjacent to the executable.
